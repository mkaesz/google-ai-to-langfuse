# --- Langfuse Cloud ---------------------------------------------------------
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_HOST=https://cloud.langfuse.com

# --- Google Cloud ------------------------------------------------------------
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# --- Pipeline (defaults shown; override if needed) ---------------------------
#PUBSUB_TOPIC=agent-designer-traces
#PUBSUB_SUBSCRIPTION=agent-designer-traces-sub
#LOG_SINK=agent-designer-to-pubsub
#LOG_NAME=agent-designer-demo
# Custom Cloud Logging filter for the sink (see setup.sh for the default):
#LOG_FILTER=
